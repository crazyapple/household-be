plugins {
    id 'java'
}

repositories {
    jcenter()
}

dependencies {
    
}

version '1.0'

sourceCompatibility = 1.8

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

wrapper {
    gradleVersion = '7.0.2' // Specify the Gradle version you want to use
    distributionType = Wrapper.DistributionType.ALL
}
